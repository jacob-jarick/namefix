#--------------------------------------------------------------------------------------------------------------
# Show Changelog
#--------------------------------------------------------------------------------------------------------------

use strict;
use warnings;

sub show_changelog 
{
	my $text = join("", &readf($main::changelog));

	&show_dialog("Changelog", $text);
}

1;
